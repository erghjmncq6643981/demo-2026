<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chandler.learning.agent.system.infrastructure.mapper.LearningSystemLogMapper">
    <insert id="insertBatch">
        INSERT INTO learning_system_log
        (id, user_id, log_type, title, detail, source, business_type, business_id,
         create_by, create_time, update_by, update_time, deleted, version)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.userId}, #{item.logType}, #{item.title}, #{item.detail}, #{item.source},
             #{item.businessType}, #{item.businessId}, #{item.createBy}, #{item.createTime}, #{item.updateBy},
             #{item.updateTime}, #{item.deleted}, #{item.version})
        </foreach>
    </insert>
</mapper>
