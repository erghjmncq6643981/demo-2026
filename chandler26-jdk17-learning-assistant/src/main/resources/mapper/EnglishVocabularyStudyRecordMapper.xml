<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chandler.learning.agent.vocabulary.infrastructure.EnglishVocabularyStudyRecordMapper">
    <insert id="insertBatchIgnore">
        INSERT INTO english_vocabulary_study_record
        (id, term, normalized_term, agent_code, template_code, provider, model_name, session_id,
         raw_content, parsed_json, token_usage, cost_time, lookup_count, last_lookup_time,
         create_by, create_time, update_by, update_time, deleted, version)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.term}, #{item.normalizedTerm}, #{item.agentCode}, #{item.templateCode},
             #{item.provider}, #{item.modelName}, #{item.sessionId}, #{item.rawContent}, #{item.parsedJson},
             #{item.tokenUsage}, #{item.costTime}, #{item.lookupCount}, #{item.lastLookupTime},
             #{item.createBy}, #{item.createTime}, #{item.updateBy}, #{item.updateTime},
             #{item.deleted}, #{item.version})
        </foreach>
        ON DUPLICATE KEY UPDATE normalized_term = normalized_term
    </insert>
</mapper>
