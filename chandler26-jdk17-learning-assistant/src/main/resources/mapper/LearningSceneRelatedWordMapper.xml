<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chandler.learning.agent.learning.infrastructure.LearningSceneRelatedWordMapper">
    <insert id="insertBatch">
        INSERT INTO learning_scene_related_word
        (id, user_id, plan_id, unit_id, scene_material_id, term, normalized_term, phonetic,
         meaning_text, context_meaning, category_code, category_name, source_type, sort_order,
         promoted, promoted_entry_id, create_by, create_time, update_by, update_time, deleted, version)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.userId}, #{item.planId}, #{item.unitId}, #{item.sceneMaterialId},
             #{item.term}, #{item.normalizedTerm}, #{item.phonetic}, #{item.meaningText},
             #{item.contextMeaning}, #{item.categoryCode}, #{item.categoryName}, #{item.sourceType},
             #{item.sortOrder}, #{item.promoted}, #{item.promotedEntryId}, #{item.createBy},
             #{item.createTime}, #{item.updateBy}, #{item.updateTime}, #{item.deleted}, #{item.version})
        </foreach>
    </insert>
</mapper>
