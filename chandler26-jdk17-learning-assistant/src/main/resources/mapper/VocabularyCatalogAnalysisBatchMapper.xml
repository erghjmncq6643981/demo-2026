<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chandler.learning.agent.mapper.vocabulary.VocabularyCatalogAnalysisBatchMapper">
    <insert id="insertBatch">
        INSERT INTO vocabulary_catalog_analysis_batch
        (id, job_id, batch_no, entry_count, entry_ids_json, status, attempt_count,
         error_message, started_time, finished_time, create_by, create_time,
         update_by, update_time, deleted, version)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.jobId}, #{item.batchNo}, #{item.entryCount}, #{item.entryIdsJson},
             #{item.status}, #{item.attemptCount}, #{item.errorMessage}, #{item.startedTime},
             #{item.finishedTime}, #{item.createBy}, #{item.createTime}, #{item.updateBy},
             #{item.updateTime}, #{item.deleted}, #{item.version})
        </foreach>
    </insert>
</mapper>
