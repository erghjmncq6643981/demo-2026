<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chandler.learning.agent.vocabulary.infrastructure.mapper.LearningVocabularyTagMapper">
    <delete id="physicalDeleteByVocabularyId">
        DELETE FROM learning_vocabulary_tag WHERE vocabulary_id = #{vocabularyId}
    </delete>
    <delete id="physicalDeleteByVocabularyIds">
        DELETE FROM learning_vocabulary_tag
        WHERE vocabulary_id IN
        <foreach collection="vocabularyIds" item="id" open="(" separator="," close=")">#{id}</foreach>
    </delete>
    <select id="selectByVocabularyIds"
            resultType="com.chandler.learning.agent.vocabulary.domain.entity.LearningVocabularyTag">
        SELECT *
        FROM learning_vocabulary_tag
        WHERE deleted = 0
          AND vocabulary_id IN
        <foreach collection="vocabularyIds" item="id" open="(" separator="," close=")">#{id}</foreach>
        ORDER BY vocabulary_id, weight DESC, tag_type ASC
    </select>
    <insert id="insertBatch">
        INSERT INTO learning_vocabulary_tag
        (id, vocabulary_id, normalized_term, tag_type, tag_value, display_name, weight, source,
         create_time, update_time, deleted, version)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.vocabularyId}, #{item.normalizedTerm}, #{item.tagType}, #{item.tagValue},
             #{item.displayName}, #{item.weight}, #{item.source}, #{item.createTime}, #{item.updateTime},
             #{item.deleted}, #{item.version})
        </foreach>
    </insert>
</mapper>
