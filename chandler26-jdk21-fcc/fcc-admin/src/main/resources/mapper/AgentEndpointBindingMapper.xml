<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chandler.fcc.admin.infrastructure.persistence.mapper.AgentEndpointBindingMapper">
  <select id="lockAgentId" resultType="long" useCache="false" flushCache="true">
    SELECT id
    FROM fcc_agent
    WHERE work_no = #{workNo}
      AND status = 'ENABLED'
      AND deleted_at IS NULL
    FOR UPDATE
  </select>
  <select id="countSwitchBlockingState" resultType="int">
    SELECT COUNT(*)
    FROM fcc_agent_presence
    WHERE agent_id = #{agentId}
      AND (active_call_id IS NOT NULL OR work_status = 'ACW')
  </select>
  <update id="deactivateByAgentId">
    UPDATE fcc_agent_endpoint_binding
    SET is_active = 0
    WHERE agent_id = #{agentId} AND is_active = 1
  </update>
  <update id="activate">
    UPDATE fcc_agent_endpoint_binding
    SET is_active = 1
    WHERE id = #{bindingId}
      AND agent_id = #{agentId}
      AND status = 'ENABLED'
  </update>
</mapper>
