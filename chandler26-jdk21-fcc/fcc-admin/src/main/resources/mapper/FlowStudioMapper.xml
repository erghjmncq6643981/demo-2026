<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chandler.fcc.admin.flow.infrastructure.FlowStudioMapper">
  <select id="activeAgentWorkNos" resultType="string">
    SELECT work_no
    FROM fcc_agent
    WHERE status = 'ENABLED'
      AND deleted_at IS NULL
      AND work_no IN
      <foreach collection="workNos" item="workNo" open="(" separator="," close=")">
        #{workNo}
      </foreach>
  </select>

  <select id="activeGroupCodes" resultType="string">
    SELECT group_code
    FROM fcc_agent_group
    WHERE status = 'ENABLED'
      AND deleted_at IS NULL
      AND group_code IN
      <foreach collection="groupCodes" item="groupCode" open="(" separator="," close=")">
        #{groupCode}
      </foreach>
  </select>

  <select id="callExists" resultType="int">
    SELECT COUNT(*)
    FROM fcc_call_session
    WHERE id = #{call}
  </select>

  <select id="instance" resultType="com.chandler.fcc.admin.flow.infrastructure.data.FlowExecutionInstanceData">
    SELECT CAST(id AS CHAR) AS id,
           CAST(call_id AS CHAR) AS callId,
           CAST(flow_version_id AS CHAR) AS versionId,
           status,
           current_step_key AS currentStep,
           context_json AS snapshot,
           started_at AS startedAt,
           ended_at AS endedAt
    FROM fcc_flow_instance
    WHERE call_id = #{call}
    ORDER BY id DESC
    LIMIT 1
  </select>

  <select id="steps" resultType="com.chandler.fcc.admin.flow.infrastructure.data.FlowExecutionStepData">
    SELECT CAST(id AS CHAR) AS id,
           step_key AS stepKey,
           action_type AS actionType,
           attempt_no AS attemptNo,
           status,
           command_id AS commandId,
           trigger_event_id AS eventId,
           input_json AS input,
           output_json AS output,
           error_code AS errorCode,
           started_at AS startedAt,
           ended_at AS endedAt,
           duration_ms AS durationMs
    FROM fcc_flow_step_execution
    WHERE call_id = #{call}
      AND id &gt; #{after}
    ORDER BY id
    LIMIT 100
  </select>
</mapper>
